package com.example.smarthome.repository.PKA_devices;

import com.example.smarthome.model.PKA_devices.THSensor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface THSensorRepository extends JpaRepository<THSensor, Long> {
}
